using UnityEngine;

[CreateAssetMenu(fileName = "NewEnemyData", menuName = "UnitDataObject/EnemyData")]
public class EnemyDataObject : UnitDataObject
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
